# 选择 Python 3.9 作为基础镜像
FROM python:3.9

# 设置工作目录
WORKDIR /app

# 复制当前目录的所有文件到容器内
COPY . .

# 安装 Python 依赖（如果有 requirements.txt）
RUN pip install --no-cache-dir -r requirements.txt

# 运行 Honeypot 监听端口 61616
CMD ["python", "honeypots_agent.py"]
